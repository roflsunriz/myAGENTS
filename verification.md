# 検証手順

## 文書の変更

このリポジトリは Markdown の指針、リンク作成用 PowerShell スクリプト、共通の GitHub Actions ワークフローを管理する。文書だけの変更に、SVG や 3D の描画テスト、新しいテスト基盤を一律に追加しない。

1. 作業前後に `git status --short --branch` を確認し、既存変更と今回の変更を区別する。
2. 共通 `AGENTS.md`、変更する追加指針、README、更新手順、変更履歴を照合する。適用対象、参照順序、完了条件が矛盾していないことを確認する。
3. 見出し、表、コードブロックを読み、相対リンクと記載したファイルパスの参照先が存在することを確認する。コピーして使う手順は、コピー先に合わせた参照の変更も説明する。
4. `git diff --check` と `git diff` を確認する。新規ファイルは差分に含まれる前にも全文を読む。ステージ後は `git diff --cached --check` と `git diff --cached` でコミット対象を確認する。
5. 今回追加・変更したルールの目的を `CHANGELOG.md` の `Unreleased` に記録する。

## Dependabot 自動処理

1. `actionlint .github/workflows/dependabot-automation.yml` で構文と式を検証する。
   このリポジトリの `CI` では、SHA-256 を照合した公式 actionlint 配布物で全ワークフローを検証する。
2. 呼び出し側の `ci_workflows` が PR で実行される workflow の表示名と一致し、すべての対象チェックが成功する前にはマージしないことを確認する。CI がないリポジトリには自動マージを設定しない。
3. 署名済み Dependabot の patch／minor は対象 SHA に資格状態を付け、major、別リポジトリの head、人間が作った PR は資格状態を付けないことを確認する。
4. CI 失敗は初回だけ failed jobs を再実行する。決定的な修復を有効にした場合は、再失敗後に読み取り権限のジョブでパッチを作り、指定された通常ファイルだけがコミットされること、古い SHA は push しないこと、修復後の `workflow_dispatch` が返す run ID の CI 成功を直接確認してからマージすることを確認する。再実行と限定修復で直らないコード・API 互換性問題は PR を残して調査する。
5. `GITHUB_TOKEN` によるマージでは通常の `push` 起点ワークフローが起動しないため、呼び出し側で必要なデプロイ workflow に `workflow_dispatch` を追加し、`post_merge_workflows` から起動されることを確認する。

## サブエージェント指針の内容確認

[AGENTS.md の「サブエージェント」](AGENTS.md#サブエージェント) と README、更新手順、変更履歴を照合し、次のケースで判断が一貫することを確認する。

| ケース | 指針に必要な挙動 |
| --- | --- |
| 安価なモデルはあるが、作業が小規模または直列である | 大幅な時間短縮の条件を満たさないため、親が直接扱う |
| 公式資料にあるモデルが、現在のツールや選択画面にはない | 利用可能と推測せず、現在選択できる候補で進める |
| ユーザーのモデル指定や設定の上書き制約がある | 指定と制約を守り、許される範囲だけで選択する |
| API 単価は分かるが、実際は定額プランを利用している | 契約の利用枠・クレジット消費条件を調べ、API 単価を実費とみなさない |
| 提供元やキャッシュ・コンテキスト長・処理モードの条件が異なる | 同じ作業に適用される課金条件と総消費量で比較し、不明な費用差を断定しない |
| 推論強度が未対応、または別モデルの同名設定しか分からない | 対応値と継承・既定値を確認し、名称だけで序列や同等性を決めない |
| 安価な候補の品質が不足する、または最大の推論強度でも改善しない | 原因を確認して指示・設定・モデル・担当を見直し、完了条件と検証を保つ |
| 新しいモデルや料金改定・提供終了の通知がある | 公式仕様と実行環境を再確認し、世代名だけで置き換えない |
| 公式の料金・提供条件を取得できない | 未確認事項を記録し、利用可能と確認済みの設定か親による直接作業で進める |

2026-09-11 に、AGENTS.md に記載した OpenAI の公式確認先の本文と、この作業で提示されたモデル・推論強度・上書き制約を照合した。公式資料では、利用可否がクライアントや認証方法などに依存すること、API 課金とプランの利用枠を区別すること、品質を満たしてから費用・速度を比較すること、推論設定を作業と対応範囲に応じて調整することを確認した。上記の判断例で、固定の価格順位・役割・推論強度の序列へ依存せず判断できることを確認した。

これは指示文の整合性確認であり、実モデルの性能比較や課金を伴うサブエージェント実行の検証ではない。実際にモデルを選ぶ作業では、その時点の公式資料と実行環境を再確認する。

## SVG 指針の内容確認

[svg/AGENTS.md](svg/AGENTS.md) を変更した場合は、次の依頼で手順を読み分けられるか確認する。

| 依頼の例 | 指針に必要な挙動 |
| --- | --- |
| 参照をそっくりにトレースする | 同じ座標・表示条件で制作途中と最終版を重ね合わせ、輪郭と主要点の差を修正する |
| 同じキャラクターの腕を曲げ、表情を変える | 部位比率を抽出し、関節、短縮、目や口の変形を体格・顔立ちの変更と区別する |
| 同じ画風で別キャラクターを描く | 線、色、陰影、比率の傾向を継承し、元の固有デザインは無条件に移植しない |
| 横顔や一部が隠れた参照から描く | 不可視の寸法を実測と偽らず、観測と推定を分けて比較する |
| 自動トレース後にパスを整理する | 最終 SVG を再描画し、元の参照と比較する。ラスターを貼っただけで完成にしない |

これは指示文の整合性確認であり、実画像による制作精度の検証ではない。SVG 制作を行ったプロジェクトでは、参照、測定基準、比率・画風の記録、表示条件、比較画像、残る差を、そのプロジェクトの検証文書に残す。指針だけを変更した場合は、画像制作での実地検証の有無を区別して報告する。

## Three.js / Blender 指針の内容確認

[threejs-blender/AGENTS.md](threejs-blender/AGENTS.md) を変更した場合は、次のケースで参照の特徴が制作都合で省略されないか確認する。

| ケース | 指針に必要な挙動 |
| --- | --- |
| 輪郭は似ているが木目・縫い目・刻印がない | 特徴一覧から不足を見つけ、実際の形状・テクスチャへ反映して拡大比較する |
| 汎用ノイズや単色で材質を代用している | 模様の位置・寸法・方向・密度と表面特性まで参照に合わせる |
| 暗い照明や遠景では似て見える | 参照と同じ視点・細部を判別できる表示条件で比較する |
| 正面は似ているが、側面・背面・上下に破綻がある | 3方向・4方向・5方向へ確認を増やし、最終版を最低5方向と死角を確認する追加視点で検証する |
| 似た角度の画像を5枚並べている | 正面・左右・背面・上方または下方を含む異なる方向を確認し、方向名とカメラ情報を記録する |
| 一方向を修正すると別方向が崩れる | 同一モデルで参照比較と全方向を再レンダリングし、方向間の視点移動を含めて退行を確認する |
| Blender では正しいが書き出すと模様が消える | ベイク・形状化・使用先の材質実装等で保持し、書き出した実ファイルを読み直して確認する |
| 保存後にテクスチャが見つからない | 素材の保存・同梱・参照先を直し、作業中の状態に依存せず再表示する |
| 一枚の参照で背面が分からない | 観測と推定を分け、見えている部分の省略には結び付けない |
| 容量制限や実装上の難しさがある | 計測と代替手段を検討し、許可なしの単純化や未達の成功扱いをしない |

これは指示文の内容確認であり、3D 制作の実地検証ではない。実際に制作した場合は、使用先での最終レンダリング、比較画像、方向名付きの多方向画像とカメラ情報、特徴一覧の確認結果と残る差を利用先に記録する。指針だけの変更では、実地検証を行ったと報告しない。

## リンク作成スクリプトの変更

- `scripts/New-CommonAgentsLink.ps1` を変更した場合は、PowerShell の構文解析でエラーがないことを確認する。
- 対象の存在、既存リンク、別のファイルが同名で存在する場合、対象外リモートの扱いを確認する。
- 適用前に `-RootPaths` で対象を限定し、`-WhatIf` で操作対象を確認する。実リンクの作成は権限のある検証用ディレクトリで行い、利用中のプロジェクトを検証のために変更しない。
- 検証できない場合は、環境・ログから分かった原因、代替確認、未検証範囲、再開条件を記録する。
