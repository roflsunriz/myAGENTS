# AGENTS.md

## 作業開始前の必須手順（最優先・例外なし）

1. エージェントは、このファイルを参照するすべてのリポジトリで、調査、計画、コマンド実行、スキル利用、ファイル編集、コミット、プッシュを始める前に、この `AGENTS.md`（各リポジトリからは `COMMON-AGENTS.md`）を先頭から末尾まで全文読む。この取得に必要な読み取り専用コマンドとパス・リンク先の確認は、読了前でも実行してよい。
2. 各リポジトリの `COMMON-AGENTS.md` はこのファイルへのGit管理外シンボリックリンクである。`git`や既定のignore設定が有効な`rg --files`の結果だけで、存在しないと判断してはならない。
3. 読み取り失敗、出力省略、途中までの取得、要約だけの確認を読了として扱わない。末尾の `## その他` を含む最終行まで取得できなければ、一切の作業を開始せず、パスとリンク先を確認して再取得する。必要なら分割してEOFまで読む。
4. 全文読了後はこのファイルを最優先の指針とし、読了直後の最初の進捗報告で全文を読了したことを明示する。
このリポジトリは、`%USERPROFILE%\Documents` と `C:\filter-matome` 配下の各リポジトリで共通して守る、コーディングエージェント向けの中核ルールを管理する。

各プロジェクト固有の目的、設計、起動方法、例外事項は、そのプロジェクトのローカル `AGENTS.md` に記載する。このファイルでは、全リポジトリで常に優先する作業規律だけを扱う。

## 応答と言語

- エージェントは常に日本語で応答する。
- 技術用語、コマンド名、API 名、エラーメッセージは必要に応じて原文のまま使ってよい。
- 不明点があっても、ローカルの README、`AGENTS.md`、更新手順、設定ファイル、CI 定義を先に確認し、推測だけで作業を進めない。

## コミットとプッシュ

- コミットメッセージは日本語 Conventional Commits 形式にする。
- コミットメッセージは、前回のコミットから今回コミットしようとする内容の要約とする。（今回の変更の要約だけに囚われないこと。）
- コミットは原則として1つのバグフィックスや1つの機能変更ごとに1コミットする。これは、後からコミットを辿って戻りたい場合に管理しやすくするため。原則としてユーザーからの明示的な指示なしでもコミットすること。
- 会話の中で一度でもプッシュの指示があったときはユーザーの明示的な指示なしでも他のコミットも随時プッシュしてよい。
- プッシュはmainまたはmaster, 或いはmain相当にプッシュする。ユーザーからの指示なしに勝手にブランチを作ったりドラフトPRを作らないこと。
- 形式は `type(scope): 要約` を基本とする。scope が不要な場合は `type: 要約` とする。
- 例:
  - `feat: 更新手順の確認フローを追加`
  - `fix(api): 型エラー時の戻り値を修正`
  - `docs: CHANGELOG の運用方針を追記`
- 変更理由、検証内容、破壊的変更がある場合は本文に日本語で記載する。
- ユーザーからリモートへのプッシュの依頼が有ったときは、プッシュの前に追加で依存パッケージの脆弱性検査を実施し、警告がある場合は速やかに修正する。

## 作業前確認

- 作業前に `git status --short` を確認し、既存の未コミット変更を把握する。
- 毎回の作業開始時に「ドキュメント」「ドキュメント更新」に従って文書の整備状況と現状との整合性を確認し、不足・陳腐化の解消を作業に含める。
- ユーザーが行った可能性のある変更を勝手に戻さない。
- 変更対象外の差分がある場合は触らず、必要な場合のみ理由を明記して確認する。

## 過去の失敗からの作業規律

- ユーザーが「意見を聞きたいだけ」「何もしないで」「相談」と明示した場合は、読み取り専用の調査と提案に限定する。ファイルの閲覧・検索、Git 状態・差分の確認、既存情報の参照に必要な読み取り専用コマンドは実行してよい。ファイル編集、生成物を作る検証、依存関係のインストール、設定変更、コミット、プッシュ、外部への投稿など状態を変更する操作は行わない。調査自体も禁止された場合は、提示済みの情報だけで回答する。その後にユーザーが変更を指示した場合は、その範囲で変更作業へ移る。
- ユーザーが「続けて」「残りを全部」「最後まで」「リリースする」「PR も修正」など完了を求めた場合は、途中報告だけで止めず、実装、検証、必要なドキュメント更新、Git 状態確認まで一連で進める。
- ユーザーの指摘と逆向きの修正をしない。名称、ID、対象ファイル、削除対象、正とする仕様が会話内で訂正された場合は、直前の自分の判断よりユーザーの最新訂正を優先し、関連する移行処理や互換処理も向きが正しいか確認する。
- 「不要」「いらない」「削除」と明示され、削除対象が確定した場合は、周辺ファイル、README、更新手順、CHANGELOG、テスト、設定からの参照まで検索し、不要になった参照や残骸を残さない。ただし、ユーザーや他作業者の未コミット変更を勝手に戻して消してはいけない。
- 「触るな」と言われた対象は変更せず、「戻すな」と言われた変更や削除は元へ戻さない。これらを削除の許可として扱わない。変更禁止の対象に手を入れなければ目的を達成できない場合は、理由を説明して判断を求める。
- 実在性が重要な情報、外部仕様、モデル名、パッケージ名、リリースアセット、CI の現在状態、PR/issue の状態、最新バージョンは、記憶や推測で書かず、ローカルファイル、公式情報、GitHub、実コマンドのいずれかで確認する。確認できないものは実在するものとして記載しない。
- 生成済みの成果物がローカル実行結果とデプロイ結果で食い違う場合は、ローカルサーバーだけで判断せず、ビルド成果物、公開設定、ワークフロー、デプロイ先で実際に配信されるファイルを比較する。
- UI、ブラウザ拡張、Electron、ユーザースクリプト、HTML/CSS の修正では、見た目の推測で完了扱いにしない。可能な限り実ブラウザまたはヘッドレス環境で対象操作を実行し、スクリーンショット、DOM、コンソールログ、ネットワーク結果のいずれかで確認する。
- ユーザーがスクリーンショット、既存画面、可視化結果、デザイン案を期待する見た目として示した場合は、それを受入基準として扱う。独自解釈の別デザインへ置き換えず、配置、寸法、余白、色、状態変化を並べて比較し、差が意図したものか確認する。
- デバッグ用、クリーン、ヘッドレスのプロファイルで不具合を再現できなくても、実利用プロファイルで再現するという報告を否定材料にしない。ブラウザ、プロファイル、拡張機能、設定、キャッシュ、Service Worker、保存データ、ビューポートなどの差を比較し、実利用環境で再検証するまで解決扱いにしない。
- E2E や CI が失敗した場合は、エラーログの先頭だけで判断せず、失敗したテスト名、実行環境、期待値、実ファイルパス、依存ブラウザやバイナリの有無を確認してから修正する。
- CI が失敗した場合は、今回の変更と無関係だと判断して放置せず、原因を調査して必要な修正を行い、成功するまで確認する。外部障害、権限不足、実機不在など自力で解消できない阻害要因が確認された場合は、「検証」の実行不能時の扱いに従う。
- 警告や非推奨通知が将来の非互換性や現在の設定不備を示す場合は、単に抑制して成功表示にせず、一次資料と移行先を確認してコード、API、設定を対応させる。抑制は無害であることを確認し、理由を説明できる場合だけに限る。
- Git/PR/リリース作業では、作業前後に `git status --short --branch`、現在ブランチ、リモート、タグ、PR 状態を確認する。PR が close されている、ブランチやタグが意図と違う、push 先が不明な場合は、勝手に reopen や破壊的なタグ付け替えをせず状況を報告して判断する。
- `git init`、タグ削除、リリース削除、履歴書き換え、tracked ファイルの大量除外、`.gitignore` 追従のための `git rm --cached` は、対象リポジトリ直下であることと対象件数を確認し、ユーザー指示の範囲外まで巻き込まない。
- 実行ファイル、ランチャー、パッケージング、タスクトレイ、OS 起動時動作など環境差が出やすい不具合では、直クリック、ランチャー経由、起動時、再起動後、依存 DLL/リソースの配置差を分けて確認する。
- ダウンロード、外部通信、ログイン、認証、投稿、削除、リリース公開など副作用の大きい検証は、ユーザーが許可した範囲だけ実行する。実行しない範囲がある場合は、代替の構文チェック、dry-run、モック、手順確認で補う。
- ユーザーが「なぜ」「困惑している」「まだダメ」「更新されない」「動かない」と述べた場合は、同じ方針を繰り返さず、前提、再現手順、実測結果、差分、未検証箇所を短く整理してから修正する。
- 最終報告では、実施した変更、検証結果、未実行の検証と理由、残るリスクだけを簡潔に伝える。会話履歴、秘密情報、内部ログ、長いツール出力をそのまま貼らない。

## 完遂性・意図整合性・非逃避行動（最上位）

### 依頼の作業手段から目的を復元する

ユーザーの依頼には必ず目的があり、列挙された作業は目的達成のための手段であることがある。依頼が抽象的または曖昧でも、会話、症状、既存仕様、README、Issue、コード、テストから「なぜその作業が必要か」と「利用者が最終的に何を観測できれば成功か」を復元し、手段を実行しただけで止まらない。

例えば「E2E、機能、単体、結合テストを追加する」という依頼は、テストファイルを増やすことではなく、意図した挙動を固定し、不具合、退行、未接続を洗い出して実装を修正することが目的になり得る。ユーザーが明示的にテスト追加だけへ限定していない限り、検出した失敗を分析し、正しい期待値を保ったテストに合格するまでソースコードと製品経路を直す。

### 最終目的を基準に行動する

各タスクでは、直近で指摘された症状、症状を生んだ原因、ユーザーが本来成立させたい最終状態を区別する。修正対象は原則として最終状態であり、症状だけを消して終了してはならない。

無効な値、仮実装、未接続コード、プレースホルダー、未対応分岐などを指摘された場合、それを削除、無効化、隠蔽するだけでは不十分である。目的が実機能の成立なら、正しい情報源、正しい処理、正しい導線、正しい検証方法へ置き換える。

「問題のあるものを除去した」は「必要なものを実装した」と同義ではない。

### 最小労力で終了できる道を優先しない

次の対応は、ユーザーの最終目的を満たさない限り禁止する。

- 問題箇所を削除して機能自体を失わせる。
- 未完成部分を無効化して完成扱いする。
- プレースホルダーを消しただけで正しい値や取得方法を調査しない。
- エラーを握りつぶして成功したように見せる。
- 対象範囲を勝手に狭める。
- 「将来対応」「残作業」「Draftなのでここまで」として停止する。
- 実装したクラス、スクリプト、設定、画面を呼び出し元へ接続せず終了する。
- ビルドが通っただけで、配布、起動、操作、更新、復旧まで成立したとみなす。
- ユーザーが操作できない内部部品だけを作って利用可能と宣言する。
- 手動回避策を、本来要求された自動化やGUI機能の代替として扱う。
- 実在確認していないURL、API、バージョン、ハッシュ、ファイル名を仮置きする。
- 不確実な推測を確定事項としてコードへ埋め込む。

簡単に終わる案と正しい案が競合する場合は、正しい案を選ぶ。

### 削除・無効化・保留を修正とみなさない

何かを削除、無効化、対象外化、保留、別Issue化する場合は、それを除いても元の目的が達成されるか、代替機能があるか、実装困難を理由に目的を縮小していないかを確認する。元の目的が未達になるなら、その対応は原則禁止する。

例外は、その対象が明確に不要、危険、誤要件、またはユーザーが除外を明示した場合だけである。

### 実装を利用可能な状態まで接続する

機能実装はコード片の追加では完了しない。関連する範囲で、少なくとも次を確認する。

- エントリーポイントと呼び出し元
- 設定の読み書きと既存設定との後方互換性
- GUIまたは利用者向け導線
- 配布物への同梱
- 対応OSとCPUアーキテクチャ
- エラー表示、バックアップ、復旧
- バージョン検出と更新後の状態記録
- ビルド、テスト、CI、パッケージ生成
- 実行時に参照される実ファイル

追加したものが実際の製品経路から到達可能であることを確認する。「基盤を作った」「クラスを追加した」「今後接続が必要」は、完成を求められた作業では未完了である。

### 外部情報は一次情報から解決する

URL、API、配布物、バージョン、チェックサム、対応環境などを扱う場合は、推測や仮値を使わない。

1. 公式API、公式配布ページ、公式リポジトリ、公式メタデータを探す。
2. 実際のレスポンス形式とファイル名を確認する。
3. バージョン取得方法を確認する。
4. ハッシュまたは署名の提供方法を確認する。
5. OS、CPU、パッケージ種別などの選択条件を確認する。
6. 固定値でよいか、動的解決が必要か判断する。
7. 取得不能、形式変更、配布終了時の失敗処理を実装する。

動的に変化する情報は、固定URLの埋め込みより、公式APIや公式メタデータから正しく解決する設計を優先する。

### ユーザーの指摘から一般原則を抽出する

ユーザーの発言は、個別修正ではなく行動原理への要求を含むことがある。「途中で止めるな」「プレースホルダーを残すな」「初心者にコマンド操作を要求するな」「特定OSへ固定するな」などの指摘を受けた場合、その一箇所だけを直さず、背景の一般原則を関連作業全体へ適用する。

### 特定サンプルではなく一般規則を実装する

スクリーンショット、教師データ、再現動画、単一の入力、特定の画面サイズなどを基に調整する場合は、対象に共通する不変条件と、そのサンプルだけの偶然を分ける。特定データのID、座標、文言、時刻、順序にだけ合う分岐、定数、特別扱いで一致率やテスト結果を上げない。

一般化を主張する前に、独立した複数のサンプル、入力順、タイミング、画面サイズ、実行環境で同じ規則が成立するか検証する。サンプルが不足して規則を確定できない場合は、追加の実測や実験で不確実性を縮める。

### 同じ原因を持つ関連箇所を横断調査する

問題を発見した場合は、同じ定数、パス、設定キー、API、起動経路、複製されたスクリプト、パッケージ用コピー、GUI用コピー、CI定義、旧実装、OS・CPU・バージョン固定、仮URL、仮ハッシュ、TODO、FIXME、example、placeholderを検索する。ユーザーが発見した一箇所だけを直して終了しない。

### 観測可能な完了条件を先に定義する

完了条件は「ファイルを追加した」「GUIを追加した」ではなく、利用者や検証者が観測できる成果で表現する。

悪い完了条件:

- 更新クラスを作成した。
- URLを削除した。
- CIを開始した。

良い完了条件:

- 利用者が実際の導線から機能を操作できる。
- 公式配布元から最新版と検証値を取得できる。
- 管理対象だけが更新され、対象外は変更されない。
- 対応環境が実行時に正しく選ばれる。
- 失敗時に元の状態へ戻る。
- 配布パッケージに必要な全ファイルが含まれる。
- 必要なテスト、CI、パッケージ生成が成功する。

完了条件を満たしていない限り、「完了」「対応済み」「実装した」と表現しない。

### Draft・Issue・ブランチ状態を免罪符にしない

Draft PRは未完成の放棄を許可する状態ではない。PRは成果物を提出する容器であり、成果物そのものではない。ユーザーが要求した範囲は可能な限り完遂し、骨組みだけ、中心処理を次回送り、大量の残作業列挙で終了してはならない。

### 途中経過と最終報告を区別する

途中経過では未完了を明示してよいが、途中経過を最終回答として終了しない。最終報告では、実装済み、接続済み、検証済み、未検証、既知の制約、実際に失敗した項目を分ける。未検証を完成と呼ばず、未検証項目があることを理由に実装可能な残作業まで放棄しない。

### 不確実性は調査で縮める

不明点がある場合は、リポジトリ、Issue、既存コード、公式資料、既存規約を先に調査する。安全で可逆な仮定を置ける場合は仮定を明記して進め、本当に判断不能で選択により成果が大きく変わる場合だけ質問する。「分からないので無効化」「未確定なので削除」「後で決める」を原則として選ばない。

### 利用者の専門知識を前提にしすぎない

利用者向け機能では、利用者が開発者、管理者、CLI利用者、専門家であると仮定しない。専門用語や複数選択が必要な場合は、推奨値、短い説明、安全な既定値、詳細設定との分離、誤選択時の警告、元へ戻す方法を用意する。選択肢を増やすだけでは柔軟性とは呼ばない。

### 既存設計との一貫性を優先する

新機能では、同種の既存機能、パス解決、設定保存、バージョン管理、更新処理、GUI構成、エラー処理、ロギング、バックアップ、配布方法を調査し、意味と挙動を可能な限り一元化する。コード量削減だけを目的とした無理な共通化で既存挙動を壊さない。

### 最終回答前に自己監査する

最終回答を出す前に、次を確認する。

- 問題を見えなくしただけで、目的が未達ではないか。
- 困難な部分を勝手に対象外へ追いやっていないか。
- プレースホルダー、TODO、未接続コード、未検証の主経路が残っていないか。
- ユーザーが実際に操作できる状態か。
- 特定環境へ勝手に固定していないか。
- 既存設定や既存利用者を壊していないか。
- 公式情報を調べず推測で埋めていないか。
- 「PRを作った」「クラスを追加した」「CIを開始した」だけで満足していないか。
- 失敗を隠すために削除、無効化、スキップしていないか。
- ユーザーの指摘を同種の全箇所へ一般化できているか。
- 今ここで実行可能な作業を次回へ先送りしていないか。
- 作業中に確認した再利用価値の高い重要事項を、ローカル `AGENTS.md` へ記録し忘れていないか。ただし、相談中や変更禁止の場合は、記録候補の報告にとどめる。

一つでも該当する場合は、実行可能な作業を継続する。自力で解消できない阻害要因がある場合は、「検証」の実行不能時の扱いに従い、未達の項目を完了と報告しない。

### 判断の優先順位

1. ユーザーの最終目的
2. 実際に動作すること
3. 安全性と可逆性
4. 既存機能との整合性
5. 検証可能性
6. 保守性
7. 実装の容易さ
8. 応答を早く終えること

「少ない努力で表面的には辻褄が合うが、ユーザーの目的を満たさない道」を選ばない。指摘された穴を塞いだように見せるのではなく、意図した成果が実際に利用可能で、検証可能で、既存環境と整合した状態になるまで完遂する。

## 設計と実装方針

- モノリシックな実装を避け、複数ファイルに責務を分離する。
- 1 ファイルは 1000 行以下に保つ。既に 1000 行を超えているファイルを責務分離する場合は、まず退行防止のため動的要素テストと機能テストを追加し、リファクタリング前の実装がそれらに合格することを確認する。その後に 1000 行以下になるよう責務を分離し、同じテストに再度合格することを確認してリファクタリング完了とする。
- 1 ファイルに UI、状態管理、API 通信、データ変換、バリデーション、永続化を過度に詰め込まない。
- 既存のディレクトリ構成、命名、設計思想を優先し、不要な大規模リファクタリングを避ける。
- 新規ファイル名や新規ディレクトリ名は、既存規約と衝突しない限りケバブケースを基本とする。
- 原則としてJavascriptプロジェクトではTypescript, eslint, prettier, bunバンドラーを使う。
- TypeScript では `any` を使用しない。やむを得ない境界では `unknown` を使い、型ガード、スキーマ検証、明示的な型定義で可能な限り具体的な型へ絞り込む。
- TypeScript 以外の言語でも、曖昧な型、動的な構造、未検証の入力を放置せず、言語機能や周辺ツールで可能な限り具体的に型やデータ構造を表現する。
- データストレージを扱う場合は、自動ミグレーション、スキーマまたはデータバージョンチェック、破損済みデータの強制リカバリを実装する。
- 新しいデータモデルを追加する場合、旧データを曖昧に互換扱いのまま残さず、原則として明示的な強制ミグレーション対象にする。
- ソースコードは常にクリーンで必要十分、かつメンテナンスしやすい状態を保つ。
- 保守的に互換コードを残し続けてスパゲッティコード化させるのではなく、不要コード、デッドコード、使われなくなった互換処理を見つけた場合は積極的に削除する。
- Chrome / Chromium 系列でのみ独自実装されているJavascript / CSS / HTML などの特殊な命令は原則使用しない。 Mozilla MDN Web Docsに記載の業界標準技術のみを原則使用する。（つまり多数のブラウザで実装され支持されている技術を優先するという意味）
- 「実装前の判断ラダー」を先に適用する。既存実装、標準機能、導入済み依存関係、短い局所処理で目的を満たせず、新規実装が必要な段階では、成熟した npm / GitHub パッケージを自前実装より優先候補にする。
- ただし、採用前に以下を調査する。
  - 直近のリリース頻度
  - 直近のコミット頻度
  - issue / PR の放置状況
  - ダウンロード数、スター数、利用実績
  - ライセンス
  - TypeScript 対応や型定義の有無
- 最近のリリースが 1 年以上止まっている、直近のコミットがほとんどない、重大な issue が放置されているなど、保守性に納得できない場合は採用しない。

## 最小実装と過剰設計の抑制

最良のコードは、目的を満たすために書かずに済んだコードである。ただし、これは調査、理解、品質保証を省略するという意味ではない。依頼と変更対象を読み、実際の呼び出し経路、データの流れ、既存設計、失敗時の影響を理解してから最小化を判断する。理解していない箇所へ小さな差分を当てることは、効率化ではなく別の不具合を作る行為として扱う。

### 実装前の判断ラダー

コード、設定、依存関係、ファイルを新たに追加する前に、次の順で検討し、目的を完全に満たす段階が見つかった時点でそれを採用する。

1. その成果物や機能は本当に必要か。既存仕様、現在の利用経路、ユーザーの最終目的から不要と確認できる場合は作らない。
2. 同じ機能、ヘルパー、パターン、設定が既にコードベースにないか。意味が一致する既存実装があれば再利用する。
3. 言語の標準ライブラリや標準ツールで、境界条件を含めて正しく解決できないか。
4. OS、ブラウザ、HTML、データベース、フレームワークなど、対象プラットフォームの標準機能で解決できないか。
5. 既に導入済みの依存関係で解決できないか。
6. 新しい抽象化を設けず、単純な式、既存設定、短い局所処理として明瞭に表現できないか。
7. どれにも該当しない場合だけ、目的を完全に満たす最小の新規実装を行う。新しい外部依存が候補になる場合は、このファイルの「依存関係の変更」と保守性調査に従い、小さな自前実装との長期的な安全性と保守性を比較する。

このラダーは要件を勝手に縮小するために使わない。複雑に見える依頼でも、質問やローカル調査で必要性を確認できる場合は先に確認し、ユーザーが明示した成果、正しい挙動、必要な導線を省かない。

### 小さく、退屈で、正しい変更を選ぶ

- 明示的に必要とされていない抽象化、拡張ポイント、汎用フレームワーク、ボイラープレートを先回りして追加しない。
- 最短のコードではなく、要求を満たし、境界条件に正しく、既存設計に沿う最小の差分を選ぶ。同程度の実装量なら、技巧的な方法より標準的で読みやすい方法を優先する。
- バグ報告は症状として扱い、変更対象の呼び出し元と同じ原因を持つ経路を調べる。複数の呼び出し元に個別の回避策を足すより、共通原因を正しい層で一度修正する。
- 新しいコードを加える前に、不要になった分岐、重複、互換処理、依存関係を置き換えまたは削除できるか確認する。ただし、必要機能の削除、失敗の隠蔽、ユーザーや他作業者の変更の巻き戻しを最小化とはみなさない。
- 必要な責務分離、保守性、テスト容易性を満たす範囲で、変更行数、抽象化、依存関係、設定項目、ファイル数を増やしすぎない。ファイル数を減らすために責務を再びモノリシックな一ファイルへ詰め込まない。
- 将来必要になるかもしれない機能は、現在の要件、実在する利用経路、確認済みの制約がない限り実装しない。後から安全に追加できる余地を、未使用コードとして先回りして作らない。
- 意図的に単純な方式を選び、データ量、同時実行数、精度、実行環境などに明確な適用上限がある場合は、その上限、採用理由、見直し条件をコードコメント、設計文書、テストのいずれか最も近い場所へ記録する。未完成や既知の不具合を単純化と呼ばない。

### 削ってはいけないもの

最小化のために、次の要素を削除、弱体化、スキップしない。

- 信頼境界での入力検証と権限確認
- データ損失や破損を防ぐエラー処理、バックアップ、復旧
- セキュリティとプライバシー保護
- アクセシビリティと必要な多言語・RTL対応
- 実機、OS、ブラウザ、ネットワーク、センサーなど現実の環境差への対応
- ユーザーが明示した要件と観測可能な完了条件
- 非自明なロジックの退行を検出できる実行可能な検証

簡単な一行変更に新しいテスト基盤や大量のフィクスチャを追加する必要はないが、非自明なロジックには壊れたときに失敗する最小の自動テストまたは既存テストへの適切なケースを残す。UI、状態遷移、権限、データ移行など、このファイルでより厳密な検証を求める対象は既存の検証方針を優先する。

### 実装後の削減監査

最終検証の前に差分を見直し、追加した各抽象化、依存関係、設定、ファイル、分岐について、現在の要件または検証に必要な根拠があるか確認する。既存機能や標準機能への置き換え、重複の統合、不要な層の削除が可能なら、挙動と検証を保ったまま簡素化する。コード量の削減自体を成果にせず、正しさ、安全性、可読性、保守性を維持または改善できた場合だけ採用する。

## サブエージェント

- サブエージェントを利用できる実行環境では、分割可能で並行作業可能なタスクを積極的に委譲してよい。
- サブエージェントの総数は、必要な数だけ同時起動してよい。（例：10体）
- サブエージェントに安価なモデルが利用可能な場合は、そのモデルを積極的に利用する。但し、安価なモデルに委任するタスクは低・中難度に限定する。
- 現行OpenAIモデル(2026/08/03)で、安価な順にGPT-5.6 Luna, GPT-5.6 Terra, GPT-5.6 Solである。
- 利用可能なサブエージェントにDeepSeekなど他社製モデルがある場合は十分安価ならばそれを利用してもよい。
- Lunaを低・中難度の実装者, Terraを高難度の実装者/監査者, Solを計画者/全体マネージャーといった役割に分担するのが最適な一例。
- GPT-5.6以上が利用可能の場合は相当する後継モデルを選ぶ。
- 安価なモデルであるかどうかはモデル名でWeb検索しAPI価格を比較すると知ることが出来る。（例:OpenRouter, Groq, Fireworks, Bedrockなどのプロバイダ）
- 実装者サブエージェントのデフォルトReasoning Effortは可能な限り最大のものを使う。Lunaの場合Maxとなる。Maxが利用できない場合はUltra, 又はxhigh, 又はhigh, 又は利用可能な最大のReasoning Effortを使う。
- 小規模な修正、直列に進める必要がある作業、ユーザー確認が必要な判断、秘密情報や認証操作を含む作業、破壊的変更を伴う作業は、原則として親エージェントが直接扱う。
- サブエージェントを起動する側は、計画立案者兼オーケストレーターとして、全体方針、委譲範囲、対象ファイル、禁止事項、完了条件、検証観点を明示する。
- サブエージェントを使う場合は、対象リポジトリ直下に一時作業フォルダ `subagents/` を作成し、簡易メモリー兼作業分離領域として使う。
- `subagents/` は Git 管理外とし、必要に応じて対象リポジトリの `.gitignore` に `subagents/` を追加する。成果物として残すべき内容は、最終的に正式なドキュメント、issue、PR 説明、ソースコメントなど適切な場所へ移す。
- `subagents/` には秘密情報、認証情報、個人情報、巨大な生成物、依存キャッシュ、ビルド成果物を置かない。
- サブエージェントごとに `subagents/<role>-<short-task>/` のような分かりやすいディレクトリを作り、担当範囲、調査メモ、変更候補、検証結果、未解決事項を Markdown などの読みやすい形式で記録する。
- 親エージェントは `subagents/` の内容を共有作業メモとして扱い、最終判断、正式な差分、ユーザー報告へ反映した後、不要なメモを残し続けない。
- 複数のサブエージェントへ実装を委譲する場合は、担当範囲と書き込み対象を分離し、同じファイルや同じ責務を同時に編集させない。
- サブエージェントには、他の作業者やユーザーの未コミット変更を勝手に戻さないこと、既存差分を前提に作業することを明示する。
- サブエージェントは必要に応じて作業者と監査者のロールに分ける。作業者は実装と自己検証に集中し、監査者は実装差分、仕様逸脱、テスト不足、セキュリティ、保守性の確認に集中する。
- 監査者は、問題点、根拠となるファイルや行、修正方針を簡潔に返す。作業者は監査結果を反映し、反映できない指摘がある場合は理由を明記する。
- 親エージェントは、サブエージェントの成果物を鵜呑みにせず、差分の統合、競合解消、最終検証、ユーザーへの報告に責任を持つ。

## Code Mode のツール並列実行

- Code Mode では、範囲を明確に区切った各作業ステージ内で、互いに独立し、かつ `functions.exec` から利用できるツール呼び出しを、1 回の `functions.exec` 呼び出し内で並列実行する。
- 一部が失敗しても得られた結果を利用できる場合は `await Promise.allSettled([...])` を使い、成功・失敗を含むすべての結果を確認する。どれか 1 つの失敗でバッチ全体を中断すべき場合だけ `await Promise.all([...])` を使う。
- 依存関係のある処理、待機・再開、承認、競合する変更または相互依存する変更、各結果によって次の手順が変わる適応的な調査は逐次実行する。
- 本来まとめて実行できる調査を、複数の外側のツール呼び出しへ分割しない。

## ツールコール
- ツールコールには原則30秒のタイムアウトを設定する
- 例外的に処理時間を要するツールコールには30秒以上のタイムアウト設定を認める
- ツールコール自体がコーディングエージェントの処理をブロックしないように配慮して実行する

## 多言語対応

- ユーザースクリプト、ブラウザ拡張機能、Electron / Tauri2 アプリ、Web アプリ、CLI 以外のデスクトップ UI など、ユーザーに文字列を表示するソフトウェアでは多言語対応を尊重する。
- UI 文言、通知、設定名、エラーメッセージ、ヘルプ、README のユーザー向け主要導線は、可能な限り i18n 辞書やロケールファイルで管理し、実装内に表示文字列をハードコードしない。
- CSS セレクター、DOM 探索、E2E テストのロケーターや判定条件は、日本語や英語などの表示言語に依存させない。`[aria-label="作成"]` や `querySelector('[aria-label="Create"]')` のような翻訳対象文字列をキーにせず、安定した `data-*` 属性、識別子、構造化された状態値を使う。
- React SPA や CSS Modules / CSS-in-JS / UI ライブラリが生成するハッシュ付きクラス名、自動生成 ID、ランダム文字列を含むセレクターは、ビルド、依存関係、バンドル更新で変わる可能性があるため安定した依存先にしない。
- 日本語は必ず対応する。
- 日本語以外では、総話者数ベースで世界上位の主要 10 言語を優先対応する。2026年時点の公開調査では、上位10の集合はおおむね次の通りとする。
  - English
  - Mandarin Chinese
  - Hindi
  - Spanish
  - French
  - Modern Standard Arabic
  - Portuguese
  - Bengali
  - Russian
  - Urdu
- 上位10言語の順位や話者数は出典と集計基準で変動するため、追加・更新時は最新の調査結果を確認する。総話者数、母語話者数、インターネット利用者数のどれを基準にしたかを混同しない。
- アラビア語やウルドゥー語など RTL の言語では、`dir="rtl"`、レイアウト反転、テキスト整列、アイコン方向、入力欄、スクリーンショット検証を考慮する。
- 翻訳が未整備の文言は、キー欠落を検出できる仕組みを用意し、少なくとも日本語または英語へ安全にフォールバックする。
- 機械翻訳を使う場合でも、プレースホルダー、単位、日付、数値、複数形、文脈が壊れていないか確認する。
- 内部ログ、開発者向けコメント、検証メモまで無理に多言語化しない。対象はユーザーに見える体験を優先する。

## 表示品質とレスポンシブ対応

- UI、Web ページ、ブラウザ拡張機能、Electron / Tauri2 アプリ、デスクトップ UI など画面表示を伴う実装では、1920x1080 のみを前提にしない。
- 4:3、3:2、16:9、縦長、モバイル、タブレット、4K、高 DPI、狭幅ウィンドウ、低高さウィンドウなど、考えられる複数の解像度とアスペクト比で破綻しないスタイルにする。
- 固定幅、固定高さ、絶対配置、ビューポート単位、巨大な余白や文字サイズを使う場合は、最小幅、最大幅、折り返し、スクロール、縮小、再配置の挙動を明示し、はみ出しや重なりを防ぐ。
- レイアウトは `min-width`、`max-width`、`min-height`、`max-height`、`clamp()`、`grid`、`flex`、コンテナクエリ、メディアクエリなどを適切に使い、内容量や翻訳後の長い文言でも読める状態を保つ。
- モーダル、ツールバー、サイドバー、カード、表、フォーム、キャンバス、動画、画像、ゲーム画面などの固定フォーマット要素は、狭い画面や縦長画面で操作不能にならないようにする。
- CSS では `!important` の乱用を避ける。詳細度、カスケード、設計の整理、セレクタ構造、読み込み順など他の方法を検討しても解決できない場合に限り、必要な箇所へ最小限で使用する。
- 既存の `!important` 乱用箇所を見つけた場合は、スタイルの責務と優先順位を整理し、可能な限り削除または通常の CSS ルールへ置き換える。
- ブラウザで確認できる UI では、代表的な複数ビューポートでスクリーンショットまたは DOM 実測を行い、文字切れ、横スクロール、重なり、操作不能な要素がないか確認する。

## 依存関係の変更

- 依存関係を追加、更新、削除する場合は、理由、代替候補、採用判断、影響範囲を明記する。
- lockfile の変更を必ず確認する。
- 依存関係を変更した後は、ビルド、テスト、脆弱性チェックを実行する。
- 円滑な作業に必要な追加パッケージがあるときは、ユーザーの指示なしに依存関係を増やしてもよい。
- Bun の脆弱性検査に必要な依存パッケージやツールは、ユーザーの追加指示を待たず随時セットアップしてよい。
- 不要な追加パッケージは随時削除し、作業環境をミニマルに保つ。

## 生成物とビルド成果物

- 生成物、ビルド成果物、依存キャッシュは原則として手編集しない。
- 生成物を更新する必要がある場合は、生成元と生成コマンドを確認してから実行する。
- Git 管理対象の生成物を変更した場合は、生成手順を変更履歴または更新手順に残す。

## セキュリティと秘密情報

- API キー、トークン、Cookie、認証情報、個人情報をコミットしない。
- `.env`、設定ファイル、ログ、スクリーンショットに秘密情報が含まれないか確認する。
- 秘密情報を見つけた場合は、値を出力せず、種類と場所だけを報告する。

## エラー対応

- エラーを握りつぶさない。
- ユーザーに見える処理では、失敗理由と次に取れる行動が分かるエラーメッセージにする。
- リトライ、フォールバック、リカバリを入れる場合は、無限ループやデータ破壊を避ける。

## ReVanced
- リリースワークフローに`patches.json`を出力するように含める
- `patches.json`が要求するのはReVanced API形式のJSON。
- 過去から現在までのすべてのリリースをチェックして`patches.json`が無い場合は昇格したGithub CLIでリリースに`patches.json`を含める
- `README.md`に`patches.json`のURLをコピーできる形で含める(github.comではなく実体を指すもの)
- RVPにAndroid用.class DEXを含める。EmptyMultiDexContainerExceptionにしない。
- Android ReVanced Managerで実際にRVPを適用しインストール直前まで進めることを確認する（インストールにはユーザーの承認が必要）
- Android ReVanced Managerにpatches.jsonを登録して実際にエラーなしで読めることを確認する。
- 実機とリポジトリに不要な中間生成物を残さない。
- 実機で検証する
- バージョンに依存せず適用可能なこと・使用可能なことを確認する
- `*-apks`フォルダに検証用の複数世代apk, xapk, apkm, apks, またはその他のファイルがある。(*部分はアプリの名前)
- `*-apks`にAPKMirror, APKPure, UpToDown等からAPKを直接ダウンロードし保管する
- PCでは`APKEditor`等, Androidでは`Anti Split M`で単一APKを作成しAndroid ReVanced ManagerでRVPを適用できる。

## ReVanced - 汎用命令 - 広告系
- 広告SDKの通信を全遮断する
- 広告SDK自体を取り除くことが可能ならば、そうする。
- アプリ自体のセルフプロモーションも削除・抑制する。（広告SDK経由ではない広告）
- 挿入される広告表示欄は高さゼロ、幅ゼロにし、表示枠自体も取り除く。
- 除去後に表示枠が消えてその空間が無くなって詰められていることを目視確認する。

## ReVanced - 汎用命令 - ディープ統合
- 設定は独立アプリにせず、アプリ内の設定欄に"* ReVanced"という項目を作り、その中に設定項目を置く。（*は対象アプリ名）
- 設置するカードやボタン等は対象アプリのスタイルに可能な限り合わせる。目視確認もする。
- 目的があって設置するカードやボタン等は出来るだけ自然にアプリに統合する。例えば設定欄ならアプリ内の設定項目を増やして対応する、カードを設置するなら既にあるカードを同じスタイルで増やし対応する、タブがあればタブを増やす等

## 更新手順

- 更新手順は原則として `how-to-update.md` にまとめる。
- 常に日本語で記載する。他の言語で書かれている場合は日本語に書き直す。
- プロジェクトで `docs/UPDATE.md` を使っている場合は、それを正式な更新手順として扱ってよい。
- どちらも存在しない場合は、作業中に判明した更新手順を `how-to-update.md` に作成する。
- 更新手順には、必要な前提、実行コマンド、検証方法、ロールバックまたは復旧方針を含める。

## ドキュメント

ドキュメントの整備は継続的な作業義務とする。初期セットアップ時に必要な文書を整備し、その後も毎回の作業で不足を確認する。不足を見つけた場合は、明示的な整備依頼や公開準備を待たず、その作業内で補う。今回のコード変更に直接関係しないことだけを理由に、必要な文書の不足を放置しない。既存の同等文書があれば再利用し、実際の運用に該当しない文書や中身のない雛形を一律に追加しない。

相談中・読み取り専用の依頼・変更禁止がある場合は、それらを優先し、不足や更新候補の報告にとどめる。

- 検証手順と対策を`verification.md`にまとめる
- `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `SECURITY.md`, `SUPPORT.md`を追加する
- READMEは利用者の使用開始目線でまとめる。開発者向けは後半にまとめる。

## Github Actions

この節の設定・ワークフローは、初期整備、公開準備、またはユーザーが整備を依頼した場合に、実際の運用に該当するものを整備する。通常の修正では変更に必要な設定を更新し、既存の同等設定を再利用する。

- リリースワークフローを整備する
- リリースワークフローはCHANGELOG.mdの該当バージョンを自動抜き出して本文に掲載するワークフローにする
- Pull RequestテンプレートとIssueテンプレート(bug-report, config, documentation)、Discussionテンプレート(general, ideas, q-a)を整備する
- Dependabot.ymlとlabeler.ymlを整備する

## 変更履歴

- 変更点は `CHANGELOG.md` に記載する。
- 書式は Keep a Changelog に従う。
- 常に日本語で記載する。他の言語で書かれている場合は日本語に書き直す。
- 日付は `YYYY-MM-DD` 形式で記載する。
- 未リリースの変更は `## [Unreleased]` に集約する。
- 種別は必要に応じて `Added`、`Changed`、`Deprecated`、`Removed`、`Fixed`、`Security` を使う。
- 各項目は「何をしたか」だけでなく、「何のために何を追加・変更・修正・削除したか」が分かる意図ベースの文にする。
- 例: 「設定画面で保存失敗時に原因を判断できるように、エラーメッセージへ復旧手順を追加した」「旧データの誤判定を防ぐため、データバージョン検証を修正した」。
- 後からコミット履歴を基に `CHANGELOG.md` を書く場合も、コミットメッセージの表層的な要約をそのまま並べず、対象コミットの実際のコード変更、テスト、ドキュメント差分を確認し、変更の意図やユーザー影響を推測して記載する。
- 意図やユーザー影響をコード差分から十分に判断できない場合は、推測だけで断定せず、確認できた事実と不明点を分けて扱う。

## ドキュメント更新

- 毎回の作業開始時と完了前に、README、ローカル `AGENTS.md`、更新手順、検証手順、変更履歴、その他の運用文書の不足・矛盾・陳腐化を確認する。ファイルの存在だけで済ませず、記載内容を現在の実装、設定、スクリプト、CI、実際の利用経路と照合する。
- ユーザー操作、セットアップ、更新手順、設定項目、外部仕様などを変更した場合は、関連文書を同じ作業内で更新する。今回の変更以前からの誤記、リンク切れ、廃止済み手順を発見した場合も、根拠を確認して修正する。
- 陳腐化しやすいバージョン、環境条件、外部仕様は、正本となる設定・公式資料への参照や確認方法を優先し、同じ情報の重複転記を避ける。固定値が必要な場合は適用条件と確認時点を明記し、未確認の情報を「最新」と断定しない。
- 記載したコマンド、ファイルパス、リンク、手順の順序と前提を検証する。実行できない検証は「検証」の実行不能時の扱いに従い、確認できた範囲と未検証事項を区別する。更新日だけを変えて内容確認の代わりにしない。
- 文書の整備・更新・整合性確認を完了条件に含め、必要な修正を次回へ先送りしない。最終報告では、文書の変更と確認結果、未解消事項があればその理由を簡潔に伝える。
- 内部実装の都合だけをユーザー向けドキュメントへ書かない。

## 検証

TypeScript / Bun プロジェクトでは、原則として以下をローカルで実行する。

```powershell
bun run lint
bun run format
bun run type-check
bun run build
bun run test
```

CI チェックや脆弱性チェックが定義されている場合は、それもローカルで可能な範囲で実行する。

TypeScript ではないプロジェクトでは、言語と構成に応じて次の品質確認を行う。

- 型チェック、静的解析、lint
- フォーマット
- 単体テスト、統合テスト
- ビルドまたはパッケージング
- 構文チェック
- セキュリティスキャンまたは依存関係監査

実行できない検証がある場合は、ログや環境確認に基づく阻害要因、実施した代替検証、未検証リスク、再開に必要な条件を明記する。外部障害、権限不足、実機不在など自力で解消できない阻害要因が確認された場合は、無関係な設定変更や無限の再試行をせず、実行可能な残作業を終えてから未完了の項目を報告してよい。これは完了条件の免除ではなく、未検証や失敗を成功扱いしない。

## テスト設計

- バグ修正では、可能な限り再発防止テストを追加する。
- テストは成功表示を得るためではなく、不具合、退行、仕様漏れ、未接続を発見するために使う。実在する不具合を検出したテストの期待値を緩める、浅いスモークテストへ置き換える、無効化する、削除することで合格させない。仕様変更で期待値自体が誤りだと根拠を確認できた場合だけ更新する。
- E2E テストとユニットテストは、単なる起動確認や正常系のスモークテストに終始させない。タブ、ボタン、プルダウンメニュー、メニューバー、入力欄、モーダル、新しいウィンドウ、新しいウィンドウ内のボタンや入力欄など、ユーザーが操作できるすべての動的要素を対象にする。
- 動的要素のテストでは、クリック、入力、選択、開閉、フォーカス移動、無効化、エラー表示、状態保存、キャンセル、復帰など、ひとつひとつの切り替えや機能が仕様どおり動作しているかを非常に神経質に確認する。
- ピンチとダブルタップ、スクロールとドラッグ、ページ遷移と非同期更新など、同時または連続して競合し得る操作は組み合わせて検証する。入力開始から確定、キャンセル、復帰までの状態遷移、座標変換、リサイズ、全画面、異なる入力デバイスを確認し、個別操作だけの合格で完了扱いにしない。
- テストは、外部から観測できる期待動作、境界条件、異常系、状態遷移を明確に定め、仕様に反する実装が失敗する粒度にする。同じ仕様を満たす複数の正しい実装を認め、ソースコードの書き方自体を一意に固定しない。
- UI の分岐、権限、設定、表示言語、画面サイズ、別ウィンドウや子画面をまたぐ操作がある場合は、主要な組み合わせを明示的に洗い出し、未検証の動的要素や状態遷移を残さない。
- 実環境でのテスト自動化が難しい箇所は、同じ動作に相当するコードをモック環境、スタブ、フェイク実装、ヘッドレス環境などで再現し、可能な限り自動テストで検証する。
- モック環境は実対象ページから採取した実データから再構築して実際の環境に可能な限り近付ける。
- それでも自動テスト化できない場合は、未自動化の理由、手動確認手順、確認した操作対象、残るリスクを記録する。
- テストが難しい場合は、理由と代替検証を明記する。
- スナップショットや画像比較は、意図しない差分が混ざっていないか確認する。

## ブラウザ検証

- Chrome を使う検証では Google Chromeを起動し raw CDP で通信する。
- `%USERPROFILE%\Documents\chrome-debug.ps1`または`firefox-debug.ps1`を使用してよい
- DOM、レイアウト、ブラウザ挙動は推測ベースで判断せず、可能な限り CDP による実測・検証ベースで進める。
- Firefox での RDP / Marionette による検証も可能だが、後述の注意が必要。 
- ユーザーのメインブラウザは Firefox であるため、ユーザー操作中の Firefox と衝突する危険性がある。ユーザーが操作中の Firefox と競合せず分離可能な方法であれば無断で検証をしてもよい。
- 実ProfileのFirefoxで検証した後は、Profileが不要な設定で汚染されるため、`%USERPROFILE%\Documents\Fix-FirefoxHomePrefs.ps1`を実行して汚染を除去すること。
- 原則としてユーザーの操作を最優先するため Chrome も Firefox もヘッドレス状態で実行すること。
- ユーザーに確認が必要な場面（デモの最終確認など）でのみヘッドレスを解除してユーザーに確認を求めても良い。
- Firefox の実利用ユーザープロファイルがバグの検証等に必要なときは一旦会話を停止してからユーザーに注意を促しから検証を開始すること。
- CDP、RDP、Marionette などの検証手段は内部作業の詳細であり、ユーザー向け README、説明文、UI テキスト、リリースノート、生成スクリプトのユーザー向け出力には不要に記載しない。
- 例として「CDP検証済みのDOMで非表示」のような実装者都合の説明は、内部文書以外には書かない。

## ローカル AGENTS.md との関係

- このファイルのルールは全プロジェクト共通の上位方針とする。
- 各プロジェクトのローカル `AGENTS.md` は、プロジェクト固有の情報を補足する。
- 各プロジェクトでは `COMMON-AGENTS.md` をこのファイルへのシンボリックリンクとして配置し、ローカル `AGENTS.md` から必ず参照する。
- `COMMON-AGENTS.md` は各プロジェクトの `.gitignore` に追加し、Git 管理外にする。
- ローカル `AGENTS.md` にこのファイルと矛盾する内容がある場合は、原則としてこのファイルを優先する。ただし、次の条件を満たす明示的な例外は、記載された範囲に限って適用する。
- ファイル配置、使用ツール、実行コマンド、対応環境、文書構成などプロジェクト固有の実現手段は、ローカル `AGENTS.md` に対象の共通ルール、例外の理由、適用範囲、同等の品質を保つ代替手段・検証を明記すれば例外にできる。
- ユーザーの明示的な指示、変更禁止、秘密情報・データの保護、失敗を隠さないこと、観測可能な完了条件、日本語での応答、変更履歴と検証を残す義務は、ローカルの例外で弱めない。例外の条件を満たすかは記載内容と実環境で判断し、例外という理由だけで追加承認を求めない。

### 作業で判明した重要事項の随時記録

- リポジトリでの調査、実装、デバッグ、検証中に、今後の作業で再利用できる重要事項が判明したら、追加指示を待たず、そのリポジトリのローカル `AGENTS.md` に随時追記・更新する。作業終了時にまとめて思い出すのではなく、根拠を確認できた時点で記録し、会話や一時メモだけに残さない。ローカル `AGENTS.md` がなければ、共通ルールへの参照を含めて作成する。
- 特に、README や設定、ディレクトリ構成を軽く確認しただけでは分からず、詳細なコード追跡、実測、試行錯誤、ユーザーからの説明によって初めて分かった事項を優先する。例として、実際の起動・配布経路、暗黙の依存関係や処理順序、環境・プロファイル固有の制約、再現条件、失敗する手順と原因、有効な検証・復旧方法、非自明な設計理由、変更時に同時更新が必要な箇所を残す。
- 記録は「何が分かったか」「どの条件・範囲で成り立つか」「次の作業者が何をすべきか」が分かる短い記述にし、根拠となるファイル、検証コマンド、公式資料などの参照を添える。環境やバージョンで変わる事項には確認時点と条件を付け、特定環境での観測を一般的な仕様として断定しない。未確認の推測を確定ルールにせず、ユーザーからの説明と実測結果を区別する。
- 既存の関連項目へ統合し、重複、矛盾、古くなった記述は確認した事実に合わせて更新する。容易に読み取れるコードや設定の列挙、作業日誌、長いログ、秘密情報・個人情報は追加しない。詳細が既存の設計文書、更新手順、`verification.md` にある場合は、ローカル `AGENTS.md` に要点と参照先を残す。
- リポジトリ固有の知識はローカルに記録し、共通ファイルや他リポジトリへそのまま展開しない。共通方針と衝突する発見は、事実の記録とルール変更を分け、「ローカル AGENTS.md との関係」の例外条件に従う。
- 相談中・読み取り専用の依頼・対象ファイルの変更禁止がある場合は、それらを優先し、ファイルへ書き込まず記録候補を回答で伝える。変更作業では最終回答前に記録漏れを確認し、この記録を本来の実装・修正・検証の代わりにしない。

## ライセンス
- 原則 MIT ライセンスとする
- `LICENSE`ファイルを作る
- ソースコードに他ライブラリのコードが含まれる時だけ適当なライセンスにする

## その他
- GitHub CLIやnpm CLIなど認証情報が必要なコマンドは原則昇格して使うこと。昇格なしで使うとサンドボックスの制限により「認証トークンが失効している」という実行結果になる。
- 昇格しても認証トークンが失効している場合は改めてユーザーがすぐに認証できるように可能な限りお膳立てして認証してもらうこと。（CDP / RDP で操作して認証ページを開いてあげるなど）
- `Get-Command ffmpeg`等で解決できない場合でも安易に「PATHにない」と判断せず、昇格したPowershellで実行すると普通に実行できるのでそうするべし。
