# Changelog

このプロジェクトの重要な変更はすべてこのファイルに記録します。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に基づきます。

## [Unreleased]

### Changed

- モデルや料金体系の変更後も適切に委譲できるように、固定のモデル価格順位・役割・後継選択を、実行環境と公式仕様・料金による選定手順へ置き換えた。API 課金とプラン利用枠の区別、推論強度の対応確認と品質に応じた調整、情報が取得できない場合の扱い、選定根拠の記録を定め、README・更新手順・検証手順にも反映した。
- 参照に一致する一方向だけで3Dモデルを完成扱いしないように、Three.js / Blender 指針へ3方向・4方向・5方向の段階的な確認と、最終版の最低5方向・死角の追加確認を導入した。方向間の視点移動、画像・カメラ情報の記録、修正・書き出し後の全方向再検証を完了条件に含めた。
- 委譲に伴う手間で作業が遅くなることを防ぐため、サブエージェントの利用を、大規模な並列実行によって起動・指示・調整・統合・検証を含む全体の所要時間を大幅に短縮できる場合のみに限定した。同時起動数と安価なモデルの選択にもこの条件を適用した。
- 必要な文書の不足や陳腐化を放置しないため、初期セットアップ時・不足発見時の整備と、毎回の作業開始時・完了前の内容照合、根拠に基づく更新・検証を共通の完了条件にした。README と更新手順にも反映し、スクリプトが存在する現状に合わせて検証の説明を修正した。
- 相談中の意図しない変更や共通ルールの解釈違いを防ぐため、読み取り専用調査の許可、削除と変更禁止の区別、文書・CI整備の適用時期、依存採用の優先順、テストの完了基準、ローカル例外と検証不能時の扱いを明確にした。
- CI の失敗を変更との関連性だけで対象外にせず解消まで進め、Bun の脆弱性検査に必要な依存やツールを追加指示なしでセットアップできるように共通規律を強化した。
- 作業開始時の共通指針見落としを防ぐため、調査やコマンド実行より前に `COMMON-AGENTS.md` を先頭から末尾まで読み、EOFを確認する必須ゲートを追加した。

### Added

- Dependabot の patch／minor PR を検証済み CI 成功後に取り込み、失敗ジョブの 1 回再実行と、指定ファイルに限定した決定的な生成物修復ができるように、権限境界を分けた再利用可能な GitHub Actions ワークフローを追加した。
- Three.js / Blender でリファレンス画像のテクスチャや細部が制作都合で省略されることを防ぐため、特徴一覧と実装の対応、同じ視点のレンダリング・拡大比較、材質と素材の保存、書き出し先での再検証を必須にする `threejs-blender/AGENTS.md` を追加。関連文書に利用・更新・検証方法を反映した。
- SVG の模写精度とポーズ・表情変更時の一貫性を検証できるように、制作途中からの重ね合わせ、部位比率の測定・再適用、遠近と表情の扱い、線・色・陰影等の画風比較、最終 SVG の表示確認を定めた `svg/AGENTS.md` を追加。README と更新手順に利用先からの参照方法と管理範囲を記載した。
- 指示文の整合性確認と実画像による制作精度の検証を混同しないように、文書・SVG 指針・リンク作成スクリプトの確認方法を `verification.md` にまとめた。
- 共通ルールで求める運用文書の不足を補うため、変更への参加、行動規範、セキュリティ、問い合わせの案内と MIT ライセンスを追加した。
- 再調査や同じ失敗の繰り返しを防ぐため、作業中に判明した非自明な前提・制約・検証方法などを、根拠と適用条件付きでローカル `AGENTS.md` に随時記録し、重複や陳腐化を整理する共通ルールを追加。相談中・変更禁止時は記録候補の報告に限定する。
- 実装量の削減を品質低下へ結び付けないため、[Ponytail](https://github.com/DietrichGebert/ponytail) の思想を基に、必要性、既存コード、標準機能、ネイティブ機能、導入済み依存関係、局所処理の順で解決策を選ぶ判断ラダーと、安全性・アクセシビリティ・検証を削らない削減監査を追加。
- 曖昧な作業指示でも背後の目的と観測可能な成功状態を復元し、テスト追加を不具合の発見と実装修正まで進める手段として扱う共通規律を追加。
- Codex Desktop と Codex VS Code Extension の保存済み主会話を再監査し、参照デザインとの比較、実利用プロファイルでの再現、特定サンプルへの過適合防止、警告の根本対応、競合操作の組み合わせ検証に関する再発防止規律を追加。
- ユーザーの最終目的を表面的な修正で取りこぼさないため、必要機能を利用可能な状態まで接続し、一次情報の確認、観測可能な完了条件、関連箇所の横断調査、最終自己監査まで完遂する共通規律を追加。
- Code Mode の独立した調査を効率よく進めつつ依存処理の順序を守るため、`functions.exec` 内の並列実行、`Promise.allSettled` と `Promise.all` の使い分け、逐次実行すべき例外を定める共通規律を追加。
- ファイルの肥大化とリファクタリングによる退行を防ぐため、1 ファイルを 1000 行以下に保ち、既存の超過ファイルは動的要素テストと機能テストを追加してから責務分離し、分離後にも再検証する共通ルールを追加。
- 共通ルールを集約する `AGENTS.md` を追加。
- リポジトリの目的と使い方を説明する `README.md` を追加。
- 共通ルールの更新手順をまとめた `how-to-update.md` を追加。
- 各リポジトリに `COMMON-AGENTS.md` シンボリックリンクを作成する `scripts/New-CommonAgentsLink.ps1` を追加。
- `COMMON-AGENTS.md` を Git 管理外にする `.gitignore` を追加。
- ケバブケース命名と `any` 回避を共通の実装方針として追加。
- データストレージのミグレーション、バージョンチェック、破損データリカバリ方針を追加。
- 新しいデータモデル追加時は旧データを原則として強制ミグレーション対象にする方針を追加。
- 不要コード、デッドコード、使われなくなった互換処理を積極的に削除するソースコード保守方針を追加。
- Chrome CDP と Firefox RDP / Marionette を使うブラウザ検証方針を追加。
- ブラウザ検証手段をユーザー向け説明へ不要に露出しない方針を追加。
- 作業前確認、依存関係、生成物、秘密情報、エラー対応、ドキュメント更新、テスト設計の共通ルールを追加。
- ユーザー向け UI で日本語と総話者数上位10言語を優先する多言語対応方針を追加。
- CSS セレクター、DOM 探索、E2E テストのロケーターや判定条件を表示言語に依存させない方針を追加。
- React SPA や CSS Modules / CSS-in-JS / UI ライブラリが生成するハッシュ付きクラス名、自動生成 ID、ランダム文字列を含むセレクターを安定した依存先にしない方針を追加。
- 複数の解像度、アスペクト比、縦長環境、モバイル、4K で破綻しない表示品質とレスポンシブ対応方針を追加。
- CSS の `!important` 乱用を避け、既存の乱用箇所を整理する方針を追加。
- サブエージェントの利用条件、委譲範囲、監査ロール、統合責任に関する共通ルールを追加。
- サブエージェント用の一時作業フォルダ `subagents/` を簡易メモリー兼作業分離領域として使う方針を追加。
- E2E テストとユニットテストで動的 UI 要素と状態遷移を網羅的に確認し、自動化困難箇所も同等動作のモック環境で検証する方針を追加。
- Codex App と Codex VS Code IDE の会話履歴分析に基づき、推測実装、検証不足、Git/PR 状態確認漏れ、ユーザー意図の取り違えを防ぐ作業規律を追加。
- 変更履歴から変更の目的とユーザー影響を読み取れるように、`CHANGELOG.md` を意図ベースで記載し、コミット履歴から作成する場合も実際のコード差分から意図を推測する方針を追加。
- サンドボックス、昇格、プッシュ、依存関係についての方針を追加
